{
  "name": "jobfit-ai-web",
  "version": "1.0.0",
  "private": true,
  "description": "JobFit AI - resume/job description match scanner. Static site + serverless functions (Netlify Functions or Vercel /api, same code).",
  "engines": {
    "node": ">=18"
  }
}
